# Agent instructions

Before creating a capability, search names, purpose and operations in this repository. Reuse or extend equivalent capabilities. Do not create organization-specific workflows. Keep manifests explicit and code deterministic. No secrets. Run `npm run check && npm test`.
