# Security

Capabilities contain no secrets. External/untrusted content remains data-only and must not become executable instructions. Mutation is denied by default and must be explicit per operation with an authorization contract. Network operations are bounded. Implementations must not log credentials or business payloads.
