const MUTATIONS=new Set(["create","update","delete"]);const OPS=new Set(["get","list",...MUTATIONS]);export async function run({operation,baseUrl,resource,authorization,payload,authorizeMutation=false,fetchImpl=fetch}){if(!OPS.has(operation))throw new Error("unsupported WordPress operation");if(!/^https?:\/\//.test(baseUrl||"")||!resource)throw new Error("baseUrl and resource required");if(MUTATIONS.has(operation)&&authorizeMutation!==true)throw new Error("WordPress mutation denied by default");const method={create:"POST",update:"PUT",delete:"DELETE"}[operation]||"GET";const r=await fetchImpl(`${baseUrl.replace(/\/$/,"")}/wp-json/${resource.replace(/^\//,"")}`,{method,headers:{...(authorization?{authorization}:{}),...(payload?{"content-type":"application/json"}:{})},body:payload?JSON.stringify(payload):undefined});const data=await r.json();if(!r.ok)throw new Error(`WordPress request failed (${r.status})`);return {ok:true,data}}
