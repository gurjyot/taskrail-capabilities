import test from 'node:test';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';test('registry governance passes',()=>{const r=spawnSync(process.execPath,['scripts/check.mjs'],{encoding:'utf8'});assert.equal(r.status,0,r.stderr)});